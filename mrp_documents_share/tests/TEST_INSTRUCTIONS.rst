Assign url document:

- Open a product that can be purchased
- In the field "Drawing" create a new entry.
- Name it "Test", open it and set "https://example.com" as url.

Vendor access:

- Create a purchase order with the selected product
- Open action > share
- Open the link in private tab
- Check if the production document link is shown beneath the product
